package com.wuji.backend.game.common

object GameConfigDefaults {
    const val DEFAULT_TOTAL_DURATION_MINUTES = 30
    const val DEFAULT_END_IMMEDIATELY_AFTER_TIME = false
    const val DEFAULT_QUESTION_FILE_PATH = "should_never_happen"
    const val DEFAULT_QUESTION_DURATION_SECONDS = 60
}
